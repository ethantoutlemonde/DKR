# services/auth-service/build-auth.yml

test-auth:
  stage: test
  image: node:18-alpine
  script:
    - cd services/auth-service
    - npm ci
    - npm test
  only:
    - develop
    - main

build-auth:
  stage: build
  image: docker:24
  services:
    - docker:24-dind
  script:
    - docker build -t $CI_REGISTRY_IMAGE/auth-service:$IMAGE_TAG --target production ./services/auth-service
    - docker tag $CI_REGISTRY_IMAGE/auth-service:$IMAGE_TAG $CI_REGISTRY_IMAGE/auth-service:latest
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker push $CI_REGISTRY_IMAGE/auth-service:$IMAGE_TAG
    - docker push $CI_REGISTRY_IMAGE/auth-service:latest
  only:
    - main

scan-auth:
  stage: security
  image:
    name: aquasec/trivy:latest
    entrypoint: [""]
  script:
    - trivy image --exit-code 0 --severity HIGH,CRITICAL $CI_REGISTRY_IMAGE/auth-service:$IMAGE_TAG
  allow_failure: true
  only:
    - main
